# CI/CD Telegram Bot

CI/CD пайплайны через Telegram бота.

## Возможности

- 🚀 Запуск CI/CD пайплайнов через Telegram команды
- 📋 Управление конфигурациями пайплайнов из GitHub репозитория
- 🐳 Выполнение задач в Docker контейнерах с ограничениями ресурсов
- 📊 Отслеживание статуса выполнения и просмотр логов
- 🔐 Авторизация по списку разрешенных Telegram ID

## Архитектура

Система состоит из:
- **Telegram Bot (Aiogram)** - интерфейс пользователя и оркестратор пайплайнов
- **Worker** - выполнение шагов в Docker контейнерах
- **GitHub API** - хранение YAML конфигураций пайплайнов
- **Логирование** - структурированные логи в файлы

## Требования

- Python 3.10+
- Docker Engine
- Telegram Bot Token (получить через [@BotFather](https://t.me/BotFather))
- GitHub Token (для доступа к репозиторию с конфигурациями)

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd cicd
```

### 2. Настройка конфигурации

Создайте файл `config/config.yaml`:

```yaml
github:
  repo_url: "https://github.com/user/repo"
  token: "${GITHUB_TOKEN}"
  pipelines_path: ".cicd/pipelines"

users:
  allowed_telegram_ids:
    - 123456789  # Замените на ваш Telegram ID
    - 987654321

docker:
  memory_limit: "512m"
  cpu_limit: "0.5"
  socket_path: "/var/run/docker.sock"

logging:
  log_dir: "./logs"
  log_file: "cicd.log"
```

Создайте файл `.env` в корне проекта:

```bash
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
GITHUB_TOKEN=your_github_token_here
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Запуск через Docker Compose

```bash
docker-compose up -d
```

Или запуск напрямую:

```bash
python -m bot.main
```

## Использование

### Команды бота

- `/start` - приветствие и проверка авторизации

### Формат YAML конфигурации пайплайна

Создайте файл `.cicd/pipelines/pipeline.yaml` в вашем GitHub репозитории:

```yaml
name: Simple Pipeline

on:
  - push

jobs:
  build:
    runs-on: docker
    image: node:18
    steps:
      - name: Checkout
        run: echo "Checking out code..."
      
      - name: Install
        run: npm install
      
      - name: Test
        run: npm test
        env:
          NODE_ENV: test
```

### Структура конфигурации

- `name` - имя пайплайна (обязательно)
- `jobs` - словарь с заданиями (обязательно)
  - `image` - базовый Docker образ для job
  - `steps` - список шагов для выполнения
    - `name` - имя шага
    - `run` - команда для выполнения
    - `env` - переменные окружения (опционально)
    - `image` - Docker образ для конкретного шага (опционально, используется из job)

## Структура проекта

```
cicd/
├── bot/                    # Telegram бот
│   ├── main.py            # Точка входа
│   ├── handlers.py        # Обработчики команд
│   ├── auth.py            # Авторизация
│   ├── orchestrator.py    # Оркестратор пайплайнов
│   └── config.py          # Загрузка конфигурации
├── worker/                 # Worker для выполнения задач
│   ├── docker_executor.py # Выполнение в Docker
│   └── job_processor.py   # Обработка заданий
├── shared/                 # Общие утилиты
│   ├── yaml_parser.py     # Парсинг YAML
│   ├── git_service.py     # Работа с GitHub API
│   ├── logger.py          # Логирование
│   └── config.py          # Конфигурация
├── config/                 # Конфигурационные файлы
│   └── config.yaml
├── logs/                   # Директория для логов
├── docker/                 # Docker файлы
│   └── Dockerfile.bot
├── docker-compose.yml
├── requirements.txt
└── README.md
```

## Безопасность

- Контейнеры запускаются без привилегий (`--privileged=false`)
- Ограничения ресурсов: память и CPU из конфига
- Авторизация по списку Telegram ID
- Переменные окружения не выводятся в логах

## Логирование

Логи сохраняются в файл `logs/cicd.log` в структурированном формате:

```
[2024-01-01 12:00:00] JOB:1 STEP:build STATUS:running
[2024-01-01 12:00:01] JOB:1 STEP:build LOG:Starting build...
[2024-01-01 12:00:05] JOB:1 STEP:build STATUS:success EXIT:0
```

## Разработка

### Локальная разработка

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите бота
python -m bot.main
```

### Тестирование

Убедитесь, что:
- Docker доступен (`docker ps`)
- Telegram Bot Token установлен
- GitHub Token имеет доступ к репозиторию
- Конфигурация `config/config.yaml` корректна
